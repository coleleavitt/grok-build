interface:
  display_name: "Grok Build"
  short_description: "Repo-specific patterns and workflows for grok-build"
  default_prompt: "Use the grok-build repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true